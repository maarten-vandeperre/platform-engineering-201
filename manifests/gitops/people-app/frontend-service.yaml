apiVersion: v1
kind: Service
metadata:
  name: people-frontend
  namespace: ${WORKSHOP_NAMESPACE}
  labels:
    app: people-frontend
    app.kubernetes.io/part-of: people-service
spec:
  selector:
    app: people-frontend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
