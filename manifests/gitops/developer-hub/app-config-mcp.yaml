backend:
  actions:
    pluginSources:
      - software-catalog-mcp-tool
      - techdocs-mcp-tool
  auth:
    externalAccess:
      - type: static
        options:
          token: "${MCP_TOKEN}"
          subject: mcp-clients
