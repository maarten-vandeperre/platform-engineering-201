[pytest]
testpaths = tests
addopts = -v --tb=short --reruns 1 --reruns-delay 15
markers =
    e2e: end-to-end browser tests against the live workshop environment
filterwarnings =
    ignore::DeprecationWarning
